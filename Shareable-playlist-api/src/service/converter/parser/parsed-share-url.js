'use strict';

export default class ParsedShareLink {
  constructor(platform, language, type, destination, url) {
    this.platform = platform;
    this.language = language;
    this.type = type;
    this.destination = destination;
    this.url = shareUrl;
  }
};
